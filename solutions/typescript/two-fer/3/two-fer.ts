/**
 * This stub is provided to make it straightforward to get started.
 */

export function twoFer(value: string = "you"): string {
  const statement = `One for ${value}, one for me.`;

  return statement;
}
